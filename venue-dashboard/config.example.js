// Copy this file to config.js (gitignored) and fill in your project's
// values. The anon key is safe to ship to the browser — access is
// enforced by venue_dashboard_stats() checking venue_owners internally.
window.RITUAL_CONFIG = {
  supabaseUrl: "https://your-project-ref.supabase.co",
  supabaseAnonKey: "your-anon-key",
};
